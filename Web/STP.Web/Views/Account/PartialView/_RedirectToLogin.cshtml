@{
    var nonceQueue = STP.Web.Helpers.NonceHelper.NonceQueue();
    foreach (var nonceValue in nonceQueue)
    {
        <script nonce="@nonceValue">
        window.location = '@Url.Action("Login", "Users")';
        </script>
    }
}